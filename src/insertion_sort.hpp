#ifndef INSERTION_SORT_HPP
#define INSERTION_SORT_HPP

// Sorts arr[left..right] (inclusive) in place using insertion sort.
// Returns the number of key comparisons performed.
long long insertionSort(int* arr, int left, int right);

#endif
